# 桌游规则遗忘点卡片库

一个纯前端本地应用，用来管理桌游收藏中的规则遗忘点、常见争议、开局准备和计分提醒。支持按最近未玩、人数范围和复杂度筛选，并在详情里展示聚会前快速复习卡片。

页面下方是「开局配件占用台」：预置三款游戏、三个桌位和共用配件。每局选定游戏、桌位并登记配件数量；同种配件被其他未结束局占用或桌位件数超限时整局拒绝、原占用不变；缺件局登记后冻结，可重启补齐；结束或取消后释放占用。在详情里改动游戏配件会重算全部未结束局，任一超限则整组退回。数据保存在浏览器本地，刷新后保留。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
